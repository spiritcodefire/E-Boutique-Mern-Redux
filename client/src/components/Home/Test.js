import React from 'react'

//simple test

const Test = (id, name, price, category ) => {
    console.log(id, name, price, category );
    return (
        <div>
            hello test
        </div>
    )
}

export default Test
